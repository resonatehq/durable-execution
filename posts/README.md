# posts

Written companions to the code, in order.

- `001-from-ephemeral-to-durable.md` — why durable promises are the primitive.

## Note on where posts live

These markdown files are the plain-text copies. The versions the site renders
are `design/content/writing/*.mdx`, which carry frontmatter, named code cards,
and the animated figures.

Right now post 001 exists in both places, so edits have to be made twice. That
should be collapsed to one source before a second post lands.
